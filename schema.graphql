type Query {
    users: [User]
    scanAllItems: Queued
    job(id: String!): Job
}

#type Mutation {
#}

type User {
    id: String!
}

type Queued {
    jobId: String!
}

type Job {
    id: String!
    state: String!
    progress: String!
    failedReason: String
}